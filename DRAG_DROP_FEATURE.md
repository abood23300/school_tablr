# خاصية السحب والإفلات (Drag & Drop) للحصص الدراسية

## نظرة عامة
تم إضافة خاصية السحب والإفلات لتمكين التعديل اليدوي على الجدول المدرسي بعد توليده تلقائياً. هذه الخاصية تسمح بإعادة ترتيب الحصص وتوزيعها بسهولة عبر واجهة بصرية تفاعلية.

## المزايا الرئيسية

### 1. عرض الحصص غير الموزعة
- **الموقع**: تظهر أعلى الجدول في وضع العرض "حسب الصف/الشعبة"
- **التصميم**: بطاقات ملونة بإطار أصفر منقط
- **المحتوى**: كل بطاقة تعرض:
  - اسم المادة
  - اسم المعلم
  - اسم الشعبة
- **العدد**: إذا كان لدى معلم 3 حصص غير موزعة من نفس المادة، تظهر 3 بطاقات منفصلة

### 2. أنماط السحب والإفلات

#### السيناريو الأول: من البطاقات إلى خلية فارغة
- **الإجراء**: سحب بطاقة من الحصص غير الموزعة → إفلات في خلية فارغة (—)
- **النتيجة**: 
  - تُملأ الخلية بالحصة
  - تختفي البطاقة من قائمة الحصص غير الموزعة
  - يتم حفظ التعديل تلقائياً
  - تحديث الإحصائيات

#### السيناريو الثاني: من البطاقات إلى خلية مشغولة (تبديل)
- **الإجراء**: سحب بطاقة → إفلات على خلية تحتوي حصة موجودة
- **النتيجة**:
  - تحل الحصة الجديدة محل الحصة القديمة
  - الحصة القديمة تعود إلى قائمة الحصص غير الموزعة كبطاقة
  - حفظ وتحديث تلقائي

#### السيناريو الثالث: من خلية إلى خلية أخرى (نقل)
- **الإجراء**: سحب محتوى خلية → إفلات في خلية فارغة
- **النتيجة**:
  - تنتقل الحصة من الخلية الأصلية إلى الخلية الجديدة
  - الخلية الأصلية تصبح فارغة (—)

#### السيناريو الرابع: من خلية إلى خلية مشغولة (تبادل)
- **الإجراء**: سحب محتوى خلية → إفلات على خلية مشغولة
- **النتيجة**:
  - الحصتان تتبادلان المواقع
  - كلا الخليتين تبقيان مشغولتين

#### السيناريو الخامس: من خلية إلى خارج الجدول (إزالة)
- **الإجراء**: سحب محتوى خلية → إفلات خارج الجدول
- **النتيجة**:
  - تُحذف الحصة من الجدول
  - تعود الحصة تلقائياً إلى قائمة الحصص غير الموزعة
  - الخلية الأصلية تصبح فارغة

## التغذية البصرية (Visual Feedback)

### أثناء السحب
- **العنصر المسحوب**: شفافية 50% (`opacity: 0.5`)
- **الخلية الفارغة**: حدود خضراء متقطعة + خلفية خضراء شفافة
- **الخلية المشغولة**: حدود برتقالية متقطعة + خلفية برتقالية شفافة

### تأثيرات Hover
- **البطاقات**: 
  - تكبير طفيف (`transform: translateY(-2px)`)
  - ظل محسّن
  - مؤشر قبض (`cursor: grab`)
- **محتوى الخلايا**: 
  - ظل خفيف
  - مؤشر قبض

## التنفيذ التقني

### الوظائف الجديدة

1. **`calculateUnassignedLessons(state, assignments)`**
   - حساب الحصص غير الموزعة من تخصيصات المعلمين
   - مقارنة المخطط (periodsPerWeek) بالفعلي (assigned count)
   - إرجاع مصفوفة من الحصص المتبقية

2. **`renderUnassignedLessons(unassigned)`**
   - عرض بطاقات الحصص غير الموزعة
   - إضافة معالجات dragstart/dragend لكل بطاقة
   - إدراج الحاوية في بداية timetableContainer

3. **معالجات الأحداث (Event Handlers)**:
   - `handleDragStart(e)`: تخزين بيانات العنصر المسحوب
   - `handleDragEnd(e)`: إزالة التأثيرات البصرية
   - `handleDragOver(e)`: إضافة التغذية البصرية حسب نوع الخلية
   - `handleDragLeave(e)`: إزالة التغذية البصرية
   - `handleDrop(e)`: تنفيذ المنطق المناسب حسب السيناريو

### التعديلات على الكود الموجود

#### في `renderTimetableByClass()`
- إضافة استدعاء `calculateUnassignedLessons()` و `renderUnassignedLessons()`
- تعديل إنشاء الخلايا:
  ```javascript
  // إضافة data attributes للخلايا
  cell.dataset.day = day;
  cell.dataset.slot = slot;
  cell.dataset.sectionId = sec.id;
  
  // إضافة معالجات الإسقاط
  cell.addEventListener('dragover', handleDragOver);
  cell.addEventListener('dragleave', handleDragLeave);
  cell.addEventListener('drop', handleDrop);
  
  // لف المحتوى في div قابل للسحب
  const wrapper = document.createElement('div');
  wrapper.className = 'lesson-cell-content';
  wrapper.draggable = true;
  wrapper.dataset.teacherId = a.teacherId;
  // ... إلخ
  ```

#### معالج الإسقاط العام (Global Drop Handler)
- مُضاف إلى `document.body` للتعامل مع السيناريو الخامس
- يكتشف الإسقاط خارج خلايا الجدول
- يحذف الحصة ويعيد عرض الجدول

## أنماط CSS المضافة

### حاوية الحصص غير الموزعة
```css
.unassigned-lessons-container {
  background: #fffbea;
  border: 2px dashed #f59e0b;
  border-radius: 8px;
  padding: 1rem;
  margin-bottom: 2rem;
}
```

### شبكة البطاقات
```css
.unassigned-lessons-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  gap: 1rem;
}
```

### بطاقة الحصة
```css
.unassigned-lesson-card {
  background: white;
  border: 2px solid #f59e0b;
  border-radius: 6px;
  padding: 0.75rem;
  cursor: grab;
  transition: all 0.2s;
}

.unassigned-lesson-card:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(245, 158, 11, 0.3);
}
```

### محتوى الخلية القابل للسحب
```css
.lesson-cell-content {
  cursor: grab;
  transition: box-shadow 0.2s;
}

.lesson-cell-content:hover {
  box-shadow: 0 0 8px rgba(0, 0, 0, 0.1);
}
```

### التغذية البصرية عند الإسقاط
```css
.drag-over-empty {
  border: 2px dashed #10b981 !important;
  background-color: rgba(16, 185, 129, 0.1);
}

.drag-over-replace {
  border: 2px dashed #f59e0b !important;
  background-color: rgba(245, 158, 11, 0.1);
}

.dragging {
  opacity: 0.5;
}
```

## قيود وملاحظات

### القيود الحالية
1. **الوضع المتاح**: الخاصية تعمل فقط في وضع "حسب الصف/الشعبة" (`by-class`)
2. **لا تتحقق من التعارضات**: لا يتم التحقق من تعارض المعلم (يمكن للمعلم أن يكون في حصتين في نفس الوقت)
3. **لا تتحقق من القيود**: لا يتم التحقق من أيام الإجازة أو القيود الأخرى
4. **الحفظ التلقائي**: كل عملية سحب وإفلات تحفظ فوراً في IndexedDB

### التحسينات المستقبلية الممكنة
1. إضافة التحقق من تعارضات المعلمين عند الإسقاط
2. إضافة رسائل تأكيد للعمليات الحساسة
3. إضافة ميزة التراجع (Undo/Redo)
4. دعم السحب والإفلات في وضع العرض حسب المعلم
5. إضافة تلميحات بصرية (tooltips) للتعليمات
6. إضافة أصوات أو رسوم متحركة للتغذية الراجعة

## سير العمل الموصى به

1. **توليد الجدول**: استخدم زر "توليد الجدول" أولاً
2. **مراجعة الحصص غير الموزعة**: انظر إلى البطاقات أعلى الجدول
3. **التوزيع اليدوي**: اسحب البطاقات إلى الخلايا الفارغة
4. **إعادة الترتيب**: اسحب الحصص بين الخلايا حسب الحاجة
5. **الحذف**: اسحب الحصص خارج الجدول لإزالتها
6. **التحقق**: راجع الإحصائيات بعد كل تعديل

## الأداء والاستجابة

- **الحفظ**: فوري في IndexedDB بعد كل عملية
- **إعادة العرض**: كاملة بعد كل إسقاط (مع الحصص غير الموزعة المحدثة)
- **الإحصائيات**: تحديث تلقائي بعد كل تعديل
- **الأداء**: سلس مع مئات الحصص بفضل استخدام event delegation

## الملفات المعدلة

1. **`js/app.js`**:
   - إضافة `calculateUnassignedLessons()`
   - إضافة `renderUnassignedLessons()`
   - إضافة معالجات drag-and-drop
   - تعديل `renderTimetableByClass()`
   - إضافة معالج global drop

2. **`styles.css`**:
   - إضافة `.unassigned-lessons-container`
   - إضافة `.unassigned-lessons-grid`
   - إضافة `.unassigned-lesson-card`
   - إضافة `.lesson-cell-content`
   - إضافة `.drag-over-empty`
   - إضافة `.drag-over-replace`
   - إضافة `.dragging`

## الاختبار

### سيناريوهات الاختبار الموصى بها
1. سحب من غير موزع → خلية فارغة ✓
2. سحب من غير موزع → خلية مشغولة (تبديل) ✓
3. سحب من خلية → خلية فارغة (نقل) ✓
4. سحب من خلية → خلية مشغولة (تبادل) ✓
5. سحب من خلية → خارج الجدول (إزالة) ✓
6. التحقق من الحفظ والتحميل بعد إعادة تحميل الصفحة ✓
7. التحقق من تحديث الإحصائيات ✓

---

**تاريخ الإضافة**: ديسمبر 2024  
**الحالة**: مكتمل وجاهز للاستخدام  
**المطور**: GitHub Copilot
