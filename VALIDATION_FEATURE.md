# ميزة التحقق من عدد الحصص

## الوصف
تم إضافة ميزة جديدة للتحقق من عدد الحصص قبل توليد الجدول المدرسي. هذه الميزة تقوم بمقارنة عدد الحصص المطلوبة مع عدد الحصص المتاحة في الأسبوع لكل شعبة.

## كيف تعمل؟

### 1. حساب الحصص المتاحة
يتم حساب إجمالي الحصص المتاحة أسبوعياً بناءً على:
- عدد أيام الدوام في الأسبوع
- عدد الحصص في اليوم الواحد

**مثال:**
- مدرسة تداوم 5 أيام × 6 حصص = 30 حصة متاحة أسبوعياً

### 2. حساب الحصص المطلوبة
يتم حساب عدد الحصص المطلوبة لكل شعبة من خلال جمع جميع الحصص المخصصة للمعلمين في تلك الشعبة.

### 3. المقارنة والتنبيهات

#### ⚠️ حالة الزيادة (Excess)
عندما تكون الحصص المطلوبة أكثر من المتاح:
```
🔴 مثال:
المطلوب: 35 حصة
المتاح: 30 حصة
الفرق: 5 حصص زائدة
```

**التنبيه:**
- سيظهر تنبيه يخبر المستخدم بعدد الحصص الزائدة
- يوضح أن بعض الحصص ستُسقط تلقائياً
- يطلب تأكيد المتابعة

#### ℹ️ حالة النقص (Deficit)
عندما تكون الحصص المطلوبة أقل من المتاح:
```
ℹ️ مثال:
المطلوب: 27 حصة
المتاح: 30 حصة
الفرق: 3 حصص فارغة
```

**التنبيه:**
- سيظهر تنبيه يخبر المستخدم بعدد الحصص الفارغة
- يوضح أن الجدول سيحتوي على حصص فراغ
- يطلب تأكيد المتابعة

## سيناريوهات الاستخدام

### سيناريو 1: توازن مثالي ✅
```
الحصص المطلوبة = الحصص المتاحة
لا يظهر أي تنبيه، يتم توليد الجدول مباشرة
```

### سيناريو 2: حصص زائدة ⚠️
```
المستخدم: يضغط على "توليد الجدول"
النظام: يعرض تنبيه بالحصص الزائدة
المستخدم: 
  - يضغط "موافق" → يولد الجدول مع إسقاط الحصص الزائدة
  - يضغط "إلغاء" → يعود لتعديل البيانات
```

### سيناريو 3: حصص ناقصة ℹ️
```
المستخدم: يضغط على "توليد الجدول"
النظام: يعرض ملاحظة بالحصص الفارغة
المستخدم:
  - يضغط "موافق" → يولد الجدول مع ترك حصص فراغ
  - يضغط "إلغاء" → يعود لتعديل البيانات
```

## مثال عملي

### إعدادات المدرسة:
- أيام الدوام: الأحد، الاثنين، الثلاثاء، الأربعاء، الخميس (5 أيام)
- حصص اليوم: 6 حصص
- **إجمالي الحصص المتاحة: 30 حصة**

### الشعب والحصص المطلوبة:
- الصف الأول أ: 28 حصة (2 حصة فراغ)
- الصف الأول ب: 32 حصة (2 حصة زائدة)
- الصف الثاني أ: 30 حصة (متوازن)

### التنبيهات المتوقعة:
```
⚠️ الصف الأول ب:
   • 2 حصة زائدة (المطلوب: 32، المتاح: 30)
   سيتم إسقاط 2 حصة تلقائياً!

ℹ️ الصف الأول أ:
   • 2 حصة فارغة (المطلوب: 28، المتاح: 30)
```

## الفوائد

1. **الشفافية:** المستخدم يعرف مسبقاً ما سيحدث
2. **تجنب المفاجآت:** لا توجد حصص مفقودة بدون تنبيه
3. **سهولة التعديل:** يمكن للمستخدم العودة وتعديل البيانات
4. **دقة البيانات:** يساعد في اكتشاف أخطاء الإدخال

## التحسينات المستقبلية المقترحة

1. إضافة اقتراحات تلقائية لحل المشاكل
2. عرض تفاصيل الحصص الزائدة (أي مادة ستُسقط)
3. إمكانية تحديد أولوية المواد عند الإسقاط
4. إشعارات لونية في واجهة المعلمين والمواد
