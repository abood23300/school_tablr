# 📚 خاصية سجل إصدارات الجداول

## نظرة عامة

تتيح هذه الخاصية **حفظ كل جدول يتم توليده تلقائيًا** مع طابع زمني، مما يوفر:
- **سجل كامل** لجميع محاولات توليد الجداول
- **إمكانية المقارنة** بين الإصدارات المختلفة
- **استعادة سريعة** لأي جدول سابق
- **تدقيق العمل** ومعرفة تاريخ التعديلات

---

## ✨ المزايا الرئيسية

### 1. **الحفظ التلقائي**
- يتم حفظ كل جدول تلقائيًا فور توليده بنجاح
- لا حاجة لأي إجراء يدوي من المستخدم
- الحفظ مع طابع زمني بالتاريخ والوقت العربي

### 2. **معلومات شاملة لكل إصدار**
كل إصدار محفوظ يحتوي على:
```javascript
{
  id: "v_1234567890",           // معرف فريد
  timestamp: 1234567890,         // الطابع الزمني الرقمي
  dateLabel: "2025-10-08 10:30 ص",  // التاريخ والوقت بالعربية
  assignments: [...],            // التخصيصات الكاملة للجدول
  stats: {                       // إحصائيات سريعة
    totalAssigned: 120,          // عدد الدروس المخصصة
    unassignedCount: 5,          // عدد الدروس غير المخصصة
    sectionsCount: 12,           // عدد الصفوف
    teachersCount: 15            // عدد المعلمين
  }
}
```

### 3. **واجهة مستخدم سهلة**
- **زر "سجل الإصدارات"** في شريط الأدوات الرئيسي
- **عرض مرئي** لجميع الإصدارات مع الإحصائيات
- **تمييز الإصدار الأحدث** بإطار أخضر
- **أزرار سريعة** للتحميل والحذف

---

## 🎯 كيفية الاستخدام

### الحفظ التلقائي
1. قم بتوليد الجدول من زر **"توليد الجدول"**
2. سيتم حفظ الإصدار **تلقائيًا** دون أي إجراء إضافي
3. ستظهر رسالة تأكيد: **"تم حفظ الجدول بنجاح ✓"**

### عرض سجل الإصدارات
1. اضغط على زر **"📚 سجل الإصدارات"** في شريط الأدوات
2. سيظهر مربع حوار يعرض جميع الإصدارات المحفوظة
3. كل إصدار يعرض:
   - 📅 **التاريخ والوقت**
   - ✓ **عدد الدروس المخصصة**
   - ⏳ **عدد الدروس غير المخصصة**
   - 📚 **عدد الصفوف**

### تحميل إصدار سابق
**الطريقة الأولى:**
- اضغط على زر **"📂 تحميل"** بجانب الإصدار المطلوب

**الطريقة الثانية:**
- اضغط على أي مكان في صندوق الإصدار نفسه

**النتيجة:**
- يتم استبدال الجدول الحالي بالإصدار المحفوظ
- تحديث جميع الإحصائيات والعرض
- ظهور رسالة تأكيد

### حذف إصدار
1. اضغط على زر **"🗑️"** بجانب الإصدار
2. تأكيد الحذف في رسالة التأكيد
3. سيتم حذف الإصدار من القائمة

**ملاحظة:** لا يمكن حذف **الإصدار الأحدث** (المميز بالإطار الأخضر)

---

## 🔧 الميزات التقنية

### تخزين البيانات
```javascript
// كل مشروع يحتوي على مصفوفة savedVersions
project = {
  id: "project_123",
  name: "مدرسة النور",
  // ... باقي البيانات
  savedVersions: [
    { id: "v_1", timestamp: 123, dateLabel: "...", assignments: [...], stats: {...} },
    { id: "v_2", timestamp: 456, dateLabel: "...", assignments: [...], stats: {...} },
    // ... حتى 20 إصدارًا
  ]
}
```

### حد التخزين
- يتم الاحتفاظ بـ **آخر 20 إصدارًا** فقط
- عند تجاوز هذا العدد، يتم **حذف الأقدم تلقائيًا**
- هذا يمنع امتلاء التخزين ويحافظ على الأداء

### النسخ العميق
- استخدام `JSON.parse(JSON.stringify(assignments))` لضمان عدم التأثر بالتعديلات اللاحقة
- كل إصدار محفوظ هو نسخة **مستقلة تمامًا**

---

## 📊 حساب الإحصائيات

### عند الحفظ
```javascript
function calculateVersionStats(assignments) {
  // 1. عدد الدروس المخصصة: طول مصفوفة التخصيصات
  totalAssigned = assignments.length;
  
  // 2. حساب الدروس غير المخصصة:
  for (كل صف) {
    for (كل مادة في الصف) {
      assigned = عدد الدروس المخصصة للمادة
      needed = lessonsPerWeek المطلوبة
      if (assigned < needed) {
        unassignedCount += (needed - assigned)
      }
    }
  }
  
  return { totalAssigned, unassignedCount, sectionsCount, teachersCount };
}
```

---

## 🎨 واجهة المستخدم

### مربع الحوار
```
┌─────────────────────────────────────────┐
│ 📚 سجل إصدارات الجداول              ×  │
├─────────────────────────────────────────┤
│ يتم حفظ كل جدول تقوم بتوليده تلقائيًا.   │
│ يمكنك تحميل أي إصدار سابق أو حذفه.      │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ [أحدث] 📅 2025-10-08 10:30 ص       │ │
│ │ ✓ 120 درس مخصص  ⏳ 5 غير مخصص     │ │
│ │                    📂 تحميل         │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 📅 2025-10-08 09:15 ص               │ │
│ │ ✓ 115 درس مخصص  ⏳ 10 غير مخصص    │ │
│ │                📂 تحميل  🗑️         │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

### الألوان والتمييزات
- **الإصدار الأحدث:** إطار أخضر (`#10b981`)، خلفية خضراء فاتحة (`#f0fdf4`)
- **الإصدارات الأخرى:** إطار رمادي (`#e5e7eb`)، خلفية بيضاء
- **عند التمرير (Hover):** ظل وانتقال سلس للأعلى

---

## 🔔 الإشعارات

### أنواع الإشعارات
1. **نجاح الحفظ:** "تم حفظ الجدول بنجاح ✓" (أخضر)
2. **نجاح التحميل:** "تم تحميل إصدار [التاريخ] ✓" (أخضر)
3. **نجاح الحذف:** "تم حذف الإصدار ✓" (أخضر)
4. **خطأ:** "الإصدار غير موجود!" (أحمر)

### الموضع والتوقيت
- **الموضع:** أعلى يمين الشاشة
- **المدة:** 3 ثوانٍ
- **الأنميشن:** انزلاق من اليمين (`slideIn`)، انزلاق لليمين (`slideOut`)

---

## 🔄 سيناريوهات الاستخدام

### السيناريو 1: التجربة والمقارنة
```
1. توليد الجدول الأول → حفظ تلقائي
2. تعديل بعض الشروط (مثل منع معلم من يوم معين)
3. توليد الجدول الثاني → حفظ تلقائي
4. فتح سجل الإصدارات
5. مقارنة الإحصائيات: أيهما أفضل؟
6. تحميل الإصدار الأفضل
```

### السيناريو 2: التدقيق والمراجعة
```
1. توليد عدة جداول في جلسة عمل واحدة
2. كل جدول محفوظ مع طابعه الزمني
3. في نهاية اليوم: مراجعة جميع الإصدارات
4. اختيار الأفضل وحذف الباقي
```

### السيناريو 3: استعادة بعد خطأ
```
1. كان لديك جدول ممتاز منذ ساعة
2. أجريت تعديلات وولدت جدولًا جديدًا
3. الجدول الجديد أسوأ!
4. فتح سجل الإصدارات
5. تحميل الجدول القديم الممتاز
```

---

## 🚀 الأداء

### التحسينات
- **النسخ العميق:** يضمن عدم التأثر بالتعديلات
- **الحد الأقصى 20 إصدار:** يمنع الامتلاء ويحافظ على السرعة
- **الحذف التلقائي للأقدم:** إدارة ذاكرة ذكية
- **الحفظ الفوري:** يتم في الخلفية دون تأخير

### حجم البيانات
- جدول متوسط (~150 درس) = ~15 كيلوبايت
- 20 إصدار = ~300 كيلوبايت
- **مقبول جدًا** للتخزين المحلي

---

## 📝 الأكواد الرئيسية

### الحفظ
```javascript
function saveCurrentTimetableVersion() {
  const st = loadState();
  const assignments = getTimetable() || [];
  if (!assignments.length) return;
  
  const now = new Date();
  const version = {
    id: `v_${now.getTime()}`,
    timestamp: now.getTime(),
    dateLabel: formatDateTime(now),
    assignments: JSON.parse(JSON.stringify(assignments)),
    stats: calculateVersionStats(assignments)
  };
  
  if (!st.savedVersions) st.savedVersions = [];
  st.savedVersions.unshift(version);
  
  if (st.savedVersions.length > 20) {
    st.savedVersions = st.savedVersions.slice(0, 20);
  }
  
  saveState({ savedVersions: st.savedVersions });
  renderVersionHistory();
  showVersionNotification('تم حفظ الجدول بنجاح ✓', 'success');
}
```

### التحميل
```javascript
function loadTimetableVersion(versionId) {
  const st = loadState();
  const version = st.savedVersions.find(v => v.id === versionId);
  if (!version) {
    showVersionNotification('الإصدار غير موجود!', 'error');
    return;
  }
  
  const assignments = JSON.parse(JSON.stringify(version.assignments));
  setTimetable(assignments);
  renderTimetableByClass(assignments);
  renderStats();
  renderFloatingUnassignedBox();
  showVersionNotification(`تم تحميل إصدار ${version.dateLabel} ✓`, 'success');
}
```

### الحذف
```javascript
function deleteTimetableVersion(versionId) {
  if (!confirm('هل أنت متأكد من حذف هذا الإصدار؟')) return;
  
  const st = loadState();
  let versions = st.savedVersions || [];
  versions = versions.filter(v => v.id !== versionId);
  
  saveState({ savedVersions: versions });
  renderVersionHistory();
  showVersionNotification('تم حذف الإصدار ✓', 'success');
}
```

---

## 🎯 الفوائد النهائية

### للمستخدم
1. ✅ **الأمان:** لن تفقد أي جدول أبدًا
2. ✅ **المرونة:** تجربة عدة خيارات دون خوف
3. ✅ **السرعة:** استعادة فورية لأي إصدار
4. ✅ **الوضوح:** معرفة تاريخ ووقت كل جدول

### للنظام
1. ✅ **التدقيق:** سجل كامل لجميع العمليات
2. ✅ **التحليل:** مقارنة الإحصائيات بين الإصدارات
3. ✅ **التوثيق:** تاريخ التعديلات محفوظ
4. ✅ **الجودة:** إمكانية الرجوع لأفضل النتائج

---

## 🔮 إمكانيات المستقبل

يمكن توسيع الخاصية مستقبلًا بإضافة:

### 1. التعليقات والملاحظات
```javascript
version = {
  // ... الحقول الحالية
  note: "جدول ممتاز بعد تقليل حصص المعلم أحمد", // ملاحظة اختيارية
  quality: 8.5 // تقييم يدوي من 1-10
}
```

### 2. المقارنة المرئية
- عرض جدولين جنبًا إلى جنب
- تمييز الفروقات بينهما بالألوان
- إحصائيات مقارنة تفصيلية

### 3. التصدير والاستيراد
- تصدير إصدار معين كملف JSON
- استيراد إصدارات من ملفات خارجية
- مشاركة الإصدارات بين المستخدمين

### 4. البحث والتصفية
- البحث بالتاريخ (من... إلى...)
- التصفية حسب الإحصائيات (مثلاً: فقط الجداول بدون دروس غير مخصصة)
- الترتيب حسب الجودة أو التاريخ

---

## 📚 ملخص

خاصية سجل الإصدارات تحول تطبيق الجداول إلى **نظام احترافي** يوفر:
- ✅ **الحفظ التلقائي** لكل جدول
- ✅ **السجل الكامل** لجميع المحاولات
- ✅ **الاستعادة السريعة** لأي إصدار
- ✅ **المقارنة السهلة** بين الخيارات
- ✅ **الإدارة الذكية** للذاكرة (حد 20 إصدار)

هذه الخاصية تمنح المستخدم **الثقة والمرونة** في التجربة والتحسين المستمر للجداول! 🎉
