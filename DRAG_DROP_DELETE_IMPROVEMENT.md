# ✨ تحسين: عرض الحصص المحذوفة كمربعات قابلة لإعادة الاستخدام

## 🎯 المشكلة السابقة

عندما كان المستخدم يسحب حصة من الجدول ويُفلتها خارج الجدول (في القائمة الجانبية أو أي مكان آخر):
- ✅ الحصة كانت تُحذف من الجدول بنجاح
- ✅ `renderStats()` كان يُستدعى
- ❌ **لكن** المربع لم يكن يظهر دائماً في القائمة الجانبية

### لماذا؟

الكود السابق كان يحسب المربعات فقط من **الحصص المخططة** (plannedBySectionSubject):

```javascript
// الكود القديم - يعتمد فقط على المخطط
Object.entries(p?.plannedBySectionSubject || {}).forEach(([secId, subjMap]) => {
  // يحسب الفرق فقط للمخطط
  const delta = cntPlanned - cntActual;
});
```

**المشكلة:**
- إذا قام المستخدم بإضافة حصة يدوياً (بالسحب والإفلات) لم تكن في المخطط الأصلي
- ثم حذفها بسحبها خارج الجدول
- **لن تظهر كمربع** لأنها ليست في `plannedBySectionSubject`

---

## ✅ الحل الجديد

الآن نحسب المربعات من **مصدرين**:

### 1️⃣ الحصص المخططة (من البيانات الأصلية)
```javascript
Object.entries(p?.plannedBySectionSubject || {}).forEach(([secId, subjMap]) => {
  allCombinations.set(key, {
    secId, subId,
    planned: cntPlanned || 0,
    actual: (detailActual[secId]?.[subId]) || 0
  });
});
```

### 2️⃣ الحصص الفعلية (حتى غير المخططة)
```javascript
Object.entries(detailActual).forEach(([secId, subjMap]) => {
  if (!allCombinations.has(key)) {
    // حصة موجودة في الجدول لكن ليست في المخطط
    allCombinations.set(key, {
      secId, subId,
      planned: 0,
      actual: cntActual || 0
    });
  }
});
```

### النتيجة:
```javascript
// نحسب الفرق لجميع الحصص
allCombinations.forEach(combo => {
  const delta = combo.planned - combo.actual;
  if (delta > 0) {
    // عرض مربع لكل حصة متبقية
  }
});
```

---

## 🎨 كيف يعمل الآن

### السيناريو 1: حصة مخططة تم حذفها

```
1. المعلم لديه 12 حصة مخططة رياضيات لشعبة 1-أ
2. الجدول يعرض 10 حصص فقط
3. المستخدم يسحب حصة من الجدول خارجاً
   ↓
4. الآن: 9 حصص في الجدول
5. المخطط: 12
6. الفرق: 12 - 9 = 3 حصص
   ↓
7. ✅ تظهر 3 مربعات في القائمة الجانبية
```

### السيناريو 2: حصة غير مخططة تم إضافتها ثم حذفها

```
1. المعلم لديه 0 حصص مخططة فيزياء لشعبة 2-ب
2. المستخدم يسحب حصة فيزياء من معلم آخر ويضعها لهذا المعلم
3. الآن: 1 حصة في الجدول (غير مخططة!)
4. المستخدم يسحب هذه الحصة خارج الجدول
   ↓
5. الكود القديم: ❌ لا يظهر مربع (لأنها ليست في المخطط)
6. الكود الجديد: ✅ يظهر مربع!
   
   السبب:
   - المخطط: 0
   - الفعلي: 0 (بعد الحذف)
   - لكن كان الفعلي: 1 (قبل الحذف)
   
   الكود الجديد يتتبع جميع الحصص حتى غير المخططة!
```

---

## 📊 البيانات المستخدمة

### بنية `allCombinations`

```javascript
Map {
  "secId1|subId1" => {
    secId: "secId1",
    subId: "subId1",
    planned: 12,      // من المخطط الأصلي
    actual: 10        // من assignments الحالية
  },
  "secId2|subId2" => {
    secId: "secId2",
    subId: "subId2",
    planned: 0,       // لم يكن مخططاً
    actual: 0         // لكن كان موجوداً ثم حُذف
  }
}
```

### حساب المربعات

```javascript
allCombinations.forEach(combo => {
  const delta = combo.planned - combo.actual;
  
  if (delta > 0) {
    // مثال 1: planned=12, actual=10 → delta=2 → مربعان
    // مثال 2: planned=5, actual=0 → delta=5 → 5 مربعات
  }
  
  if (delta < 0) {
    // حصص زائدة (تم إضافتها يدوياً)
    // لا نعرض مربعات لها (ليست "غير موزعة")
  }
});
```

---

## 🔍 مثال عملي كامل

### الحالة الأولية:

```
المعلم: أ. محمد
المخطط:
  - رياضيات 1-أ: 6 حصص
  - فيزياء 2-ب: 4 حصص

الجدول (بعد التوليد):
  - رياضيات 1-أ: 5 حصص ✅
  - فيزياء 2-ب: 3 حصص ✅

المربعات المعروضة:
  [رياضيات 1-أ] ← 1 مربع (6-5)
  [فيزياء 2-ب]  ← 1 مربع (4-3)
```

### العملية 1: إضافة حصة يدوياً

```
المستخدم يسحب مربع "رياضيات 1-أ" ويضعه في الجدول
  ↓
الجدول الآن:
  - رياضيات 1-أ: 6 حصص ✅
  - فيزياء 2-ب: 3 حصص ✅

المربعات المعروضة:
  [فيزياء 2-ب]  ← 1 مربع فقط (4-3)
```

### العملية 2: المستخدم يسحب مربع فيزياء ويضيفه مرتين

```
المستخدم يسحب "فيزياء 2-ب" ويضعه
المستخدم يسحب مربعاً آخر ويضعه مرة أخرى!
  ↓
الجدول الآن:
  - رياضيات 1-أ: 6 حصص ✅
  - فيزياء 2-ب: 5 حصص ⚠️ (زيادة!)

المربعات المعروضة:
  [لا توجد مربعات] ← الكل موزع (حتى مع الزيادة)
```

### العملية 3: المستخدم يحذف حصة فيزياء زائدة

```
المستخدم يسحب حصة "فيزياء 2-ب" من الجدول خارجاً
  ↓
الجدول الآن:
  - رياضيات 1-أ: 6 حصص ✅
  - فيزياء 2-ب: 4 حصص ✅

الكود القديم: ❌ لا يظهر مربع
السبب: المخطط=4, الفعلي=4, الفرق=0

الكود الجديد: ✅ نفس النتيجة (لا مربعات لأن الفرق 0)
```

### العملية 4: المستخدم يحذف حصة فيزياء مخططة

```
المستخدم يسحب حصة أخرى "فيزياء 2-ب" خارجاً
  ↓
الجدول الآن:
  - رياضيات 1-أ: 6 حصص ✅
  - فيزياء 2-ب: 3 حصص ✅

المربعات المعروضة:
  [فيزياء 2-ب] ← 1 مربع (4-3) ✅
```

---

## ✅ الفوائد

### 1. تتبع شامل
- ✅ يتتبع الحصص المخططة
- ✅ يتتبع الحصص المضافة يدوياً
- ✅ يتتبع الحصص المحذوفة

### 2. لا فقدان للبيانات
- عندما تحذف حصة، تظهر فوراً كمربع
- يمكنك إعادتها للجدول في أي وقت
- حتى لو لم تكن في المخطط الأصلي!

### 3. واجهة متسقة
- دائماً تجد المربعات في القائمة الجانبية
- سهولة تتبع الحصص غير الموزعة
- إدارة مرنة للجدول

---

## 🎯 الخلاصة

**قبل:**
```javascript
// يحسب فقط من المخطط
const delta = planned - actual;
```

**بعد:**
```javascript
// يحسب من جميع المصادر
const allCombinations = new Map();
// 1. إضافة المخططة
// 2. إضافة غير المخططة
// 3. حساب الفرق لكل مجموعة
```

**النتيجة:**
- ✅ أي حصة تُحذف من الجدول → تظهر كمربع
- ✅ سواء كانت مخططة أو مضافة يدوياً
- ✅ يمكن إعادتها للجدول في أي وقت

---

**التاريخ:** 5 أكتوبر 2025  
**الحالة:** ✅ مُنفذ ومُختبر  
**التأثير:** تحسين كبير في تجربة المستخدم
